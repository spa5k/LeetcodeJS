/**
 * Returns the minimum window in the original string that contains all characters in the check string.
 * @param {string} original - The original string.
 * @param {string} check - The check string.
 * @returns {string} - The minimum window in the original string that contains all characters in the check string.
 */
function getMinimumWindow(original, check) {}
console.log(getMinimumWindow('cdbaebaecd', 'abc'));
